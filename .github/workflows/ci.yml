name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest

    # Updating a versioned bundle writes the cached model into the generated project's own
    # database, which is what `CachedModelWriter` exists to do, so the suite needs a server to
    # write it to. Host and credentials are the framework's defaults: 127.0.0.1, root, no password.
    services:
      mariadb:
        image: mariadb:11
        env:
          MARIADB_ALLOW_EMPTY_ROOT_PASSWORD: "yes"
        ports:
          - 3306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - uses: actions/checkout@v5

      - name: Check out Foundation
        uses: actions/checkout@v5
        with:
          repository: dantesabatier/Foundation
          ref: master
          path: stack/Foundation
      - name: Check out CoreData
        uses: actions/checkout@v5
        with:
          repository: dantesabatier/CoreData
          ref: master
          path: stack/CoreData
      - name: Check out Service
        uses: actions/checkout@v5
        with:
          repository: dantesabatier/Service
          ref: master
          path: stack/Service

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.5"
          extensions: json, gettext, intl, dom, curl, mbstring, redis, pdo, pdo_mysql
          ini-values: zend.assertions=1
          coverage: none

      - name: Resolve the stack from the sibling checkouts
        run: |
          composer config repositories.foundation --json '{"type":"path","url":"stack/Foundation","options":{"symlink":false,"versions":{"sabatier/foundation":"1.0.0"}}}'
          composer config repositories.coredata --json '{"type":"path","url":"stack/CoreData","options":{"symlink":false,"versions":{"sabatier/coredata":"1.0.0"}}}'
          composer config repositories.service --json '{"type":"path","url":"stack/Service","options":{"symlink":false,"versions":{"sabatier/service":"1.0.0"}}}'

      - name: Install dependencies
        run: composer update --prefer-dist --no-interaction --no-progress

      - name: Wait for MariaDB
        run: |
          for i in $(seq 1 30); do
            mysqladmin ping -h 127.0.0.1 -u root --silent && exit 0
            sleep 2
          done
          echo "MariaDB did not become ready" >&2
          exit 1

      - name: Run the test suite
        run: vendor/bin/phpunit --no-coverage

  analysis:
    name: Static analysis
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Check out Foundation
        uses: actions/checkout@v5
        with:
          repository: dantesabatier/Foundation
          ref: master
          path: stack/Foundation
      - name: Check out CoreData
        uses: actions/checkout@v5
        with:
          repository: dantesabatier/CoreData
          ref: master
          path: stack/CoreData
      - name: Check out Service
        uses: actions/checkout@v5
        with:
          repository: dantesabatier/Service
          ref: master
          path: stack/Service

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.5"
          extensions: json, gettext, intl, dom, curl, mbstring, redis, pdo, pdo_mysql
          ini-values: zend.assertions=1
          coverage: none

      - name: Resolve the stack from the sibling checkouts
        run: |
          composer config repositories.foundation --json '{"type":"path","url":"stack/Foundation","options":{"symlink":false,"versions":{"sabatier/foundation":"1.0.0"}}}'
          composer config repositories.coredata --json '{"type":"path","url":"stack/CoreData","options":{"symlink":false,"versions":{"sabatier/coredata":"1.0.0"}}}'
          composer config repositories.service --json '{"type":"path","url":"stack/Service","options":{"symlink":false,"versions":{"sabatier/service":"1.0.0"}}}'

      - name: Install dependencies
        run: composer update --prefer-dist --no-interaction --no-progress

      - name: Psalm
        run: vendor/bin/psalm --output-format=github --no-progress

      # Advisory, as CONTRIBUTING says: Rector proposes modernisations and not every
      # proposal is wanted, so its report informs a review rather than blocking one.
      - name: Rector
        continue-on-error: true
        run: vendor/bin/rector process src --dry-run --no-progress-bar
