/**
 * Renderer bundle for Electron Forge + Vite. The main window loads the PHP dev server
 * (see main.ts); this entry exists so `npm start` / packaging have a valid renderer target.
 */
import "./renderer.css"
