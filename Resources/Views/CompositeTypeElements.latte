{templateType App\ViewControllers\EditorController}
{define compositeTypeElements}
    <div class="table-responsive">
        <table class="table table-hover table-sm caption-top">
            <caption class="fw-bold text-shadow">Attributes</caption>
            <thead>
            <tr>
                <th scope="col" class="col-sm-2 text-shadow">Name</th>
                <th scope="col" class="col-sm-2 text-shadow">Type</th>
                <th scope="col" class="col-sm-8 text-shadow"></th>
            </tr>
            </thead>
            <tbody>
            <tr n:foreach="$selectedCompositeType->elements as $element" id="property-{$element->objectID->referenceObject}" data-href="/Editor?project={$project?->objectID?->referenceObject}&composite={$selectedCompositeType->objectID->referenceObject}&property={$element->objectID->referenceObject}" n:class="$element->objectID->referenceObject === $selectedProperty?->objectID?->referenceObject ? table-active">
                {var string $img = img($element)}
                <td class="text-truncate">
                    <img class="ps-2" src="/Resources/Images/{$img}@1x.png" srcset="/Resources/Images/{$img}@2x.png 2x, /Resources/Images/{$img}@3x.png 3x" alt="">
                    {$element->name}
                </td>
                <td>
                    <form action="/Attribute" method="POST">
                        <input type="hidden" name="X-Http-Method-Override" value="PATCH">
                        <input type="hidden" name="objectID" value="{$element->objectID->referenceObject}">
                        <select name="type" id="type" class="form-select form-select-sm transparent border-0 py-0 w-auto" aria-label="Type" data-autosave="true">
                            <option n:foreach="$attributeTypes as $attributeType" value="{$attributeType->value}" {$attributeType->value === $element->type->value ? selected}>
                                {$attributeType->name}
                            </option>
                        </select>
                    </form>
                </td>
                <td></td>
            </tr>
            </tbody>
            <tfoot>
            <tr>
                <td>
                    <div class="d-inline-block">
                        <button class="btn icon p-0" onclick="add({App\Model\Attribute::entity()->name}, {App\Model\Attribute::entity()->name|firstLower}, {$selectedCompositeType}, {$selectedCompositeType?->elements?->count}, register)">
                            <i class="bi bi-plus"></i>
                        </button>
                        <button class="btn icon p-0" onclick="remove({$selectedProperty}, register)" {!$selectedProperty instanceof App\Model\Attribute ? disabled}>
                            <i class="bi bi-dash"></i>
                        </button>
                    </div>
                </td>
                <td></td>
                <td></td>
            </tr>
            </tfoot>
        </table>
    </div>
{/define}
